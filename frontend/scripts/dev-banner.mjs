/* eslint-disable no-console */
console.log(`
  DashFlow frontend (Next.js)
  — Open the "Local" URL printed below in your browser.
  — This terminal stays busy until you stop the server (Ctrl+C).
  — If port 3000 is in use, Next.js may choose 3001 (watch the URL line).
`);
